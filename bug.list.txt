# bug.list.txt — Known bugs & issues
# Format: [status] description
#   status: OPEN | FIXED | WONTFIX
# Add new bugs at the top. Move FIXED/WONTFIX to bottom.


[FIXED] BUG-012: ipLimiter background reaper ticker leak
  Fixed: 2026-07-27 — Add stopCh, Stop() method, and MemGate.Close() to cleanly terminate ipLimiter reaper ticker loop.

[FIXED] BUG-011: Hardcoded .localhost subdomain check for SSE status route
  Fixed: 2026-07-27 — Replace .localhost domain check with resolveSubdomain(r) to generate correct SSE /~status URLs on production domain deployments.

[FIXED] BUG-010: Listener channel and goroutine leak in DownloadManager
  Fixed: 2026-07-27 — Fix RemoveListener channel closing, add AddListenerWithContext for SSE disconnect auto-cleanup, and clear listeners on job finish.

[FIXED] BUG-009: Memory & Client IP leak in RefererTracker
  Fixed: 2026-07-27 — Replace map clear with LRU eviction for pathMIDs; bound recentMIDs client IPs & add auto-prune TTL.

[FIXED] BUG-008: Swarm fallback & automatic re-announcements on node restart
  Fixed: 2026-07-26 — MemHerald background re-announcements & fallback to connected swarm peers when DHT providers expire.

[FIXED] BUG-007: Resumable activity-based timeouts for large dataset streaming
  Fixed: 2026-07-26 — ActivityContext with 60s idle timeout & cancelOnCloseReader in Memex v2 sessions.

[FIXED] BUG-006: Gateway/node API hang on non-existent or remote MIDs
  Fixed: 2026-07-21 (5886686) — Add local Stat check to RefererTracker fallback;
  Use pebble.NoSync and close committed batches in PebbleDatastore.

[FIXED] BUG-005: Desktop must not auto-start daemon; Start/Stop button only
  Fixed: 2026-07-11 — removed Go autoStartDaemon + frontend ensureAutoStart;
  AutoStart forced false on load.

[FIXED] BUG-004: Desktop auto-start daemon on GUI launch unreliable
  Superseded by BUG-005 (auto-start removed by design).

[FIXED] BUG-003: Desktop "Check Updates" fails with GitHub API rate limit
  Fixed: 2026-07-11 — non-API-first (redirect → atom → API last) + 30m cache.

[FIXED] BUG-002: Desktop explorer iframe opened every link in system browser
  (including in-app /explorer/* SPA route changes). Interceptor now only
  forwards external / target=_blank / download / non-explorer URLs.
  Fixed: 2026-07-11 — gateway/explorer/explorer.go inject + desktop main.js.

[FIXED] BUG-001: Root URL serves leaked MID content via RefererTracker state
  Fixed: 2026-07-09 — skip referer resolution for root, remove cookie fallback,
  scope recentMIDs per client IP. Commit on features branch.
